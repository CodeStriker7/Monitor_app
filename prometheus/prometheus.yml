global:
  scrape_interval: 5s # Har 5 sekundda ma'lumot yig'ib turadi

scrape_configs:
  - job_name: 'system-monitor-job'
    static_configs:
      - targets: ['monitor-app:80'] # 'monitor-app' bu docker-compose dagi servis nomi