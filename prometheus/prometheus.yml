global:
  scrape_interval: 5s # Har 5 sekund

scrape_configs:
  - job_name: 'system-monitor-job'
    static_configs:
      - targets: ['monitor-app:8080'] # 'monitor-app'  docker-compose dagi servis nomi